<li>
	{{name}}

	<button class="change-consume" data-id="{{id}}" data-newconsume="{{consume}}">
		{{#if consume}}EAT THIS!{{else}}ORDER THIS!{{/if}}
	</button>

	<button class="delete-burger" data-id="{{this.id}}">THIS BURG IS TRASH!</button>
</li>
